---
label: Template collections expose ordered manuscripts plus spine records through helpers such as `groupBy(...)` and `splitBy(...)`.
---

# Template collections expose ordered manuscripts plus spine records through helpers such as `groupBy(...)` and `splitBy(...)`.

- Template collections expose ordered manuscripts plus spine records through helpers such as `groupBy(...)` and `splitBy(...)`.
- Use `splitBy(...)` when build structure should follow ordered boundary changes.
- Use `groupBy(...)` when you want unordered summary buckets.
- Related configuration: CFG-TEMPLATES.
- Related concepts: CON-TEMPLATE-ENGINE.
